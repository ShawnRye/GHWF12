
def compute_hw_average(grades):
    """
    Compute the averagr grade for the list of assignment grades.
    Param: list of grades (all floats)
    Return:  
   """
    if len(grades) == 0:
        return 0
    return grades[0]
